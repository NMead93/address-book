var example = new Vue({
    el: '#example',
    data: {
        name: 'Name',
        number: "Number",
        address: "Address"
    }
})

var list = new Vue({
    el: '#list',
    data: {
        seen: true
    }
})
