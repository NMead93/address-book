@{
    Layout = "Shared/_Layout.cshtml";
}
@if (M)
<ul>
    @foreach (var contact in Model)
    {
        <li><a href="/contact/@contact.GetName()">@contact.GetName()</a></li>
    }
</ul><br>

<a href="/new">Add New Contact</a>
